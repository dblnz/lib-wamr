menuconfig LIBWAMR
	bool "wamr - Intel's WebAssembly Micro Runtime version 2.4.1"
	default y
	select LIBMUSL
	select LIBLWIP
	select LIBUKTIME
	select UKUNISTD

if LIBWAMR
config LIBWAMR_MAIN_FUNCTION
	bool "Provide main function"
	default y

config LIBWAMR_BUILD_HEAP_POOL_SIZE
    int "WAMR build heap pool size"
    default 262144
endif
